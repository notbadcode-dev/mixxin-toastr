export const TIMEOUT_LIMIT = {

    DEFAULT_TIMEOUT: 5000,
    MIN_TIMEOUT: 0,
    MAX_TIMEOUT: 36000,
    SUM_TIMEOUT: 1000,

    DEFAULT_EXTENDED_TIMEOUT: 1000,
    MIN_EXTENDED_TIMEOUT: 0,
    MAX_EXTENDED_TIMEOUT: 6000,
    SUM_EXTENDED_TIMEOUT: 100, 
}