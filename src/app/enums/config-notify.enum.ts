export enum ProgressAnimationType {
    decreasing = 'decreasing',
    increasing = 'increasing',
}

export enum TimeOutOptions {
    onlyTimeout = 'timeOut',
    onlyExtendedTimeOut = 'extendedTimeOut'
}

export enum ToastType {
    info = 0,
    success = 1,
    warning = 2,
    danger = 3
}